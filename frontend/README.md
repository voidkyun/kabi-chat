# Frontend Workspace

`frontend/` は React SPA を配置する領域です。

## Directory Intent

- `src/routes/`
  - 認証完了後のメイン画面と画面遷移
- `src/features/`
  - `auth`, `workspaces`, `channels`, `messages`, `macros`
- `src/shared/`
  - `layout`, `form`, `renderer`

MVP の実装詳細は `docs/architecture/frontend.md` を参照してください。
